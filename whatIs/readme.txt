NOTE : To use the accelerator properly, you need to have accelerate installed (pip3 install accelerate)

then configure it : accelerate config

Then either run : accelerate launch main.py
Or : just python3 main.py

